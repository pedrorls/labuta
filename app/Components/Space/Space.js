import React from 'react';
import { View } from 'react-native';
import styles from './styles';


export default Space = () => {
    return(
        <View
            style={ styles.space }
        />
    );
}